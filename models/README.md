# Models

Do not commit model weights here.

Use this directory for small metadata files only. Fine-tuned checkpoints should be written to:

- `outputs/structura/<run-name>`
- an external server volume
- Hugging Face Hub
